import React from 'react';

export const PrevButton = (props: any) => {
  return <button className="btn prev-btn" {...props} >Prev</button>;
}

export const NextButton = (props: any) => {
  return <button className="btn next-btn" {...props}>Next</button>
}
